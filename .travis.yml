language: python
python:
    - "3.4"
    - "3.5"
    - "3.6"

install:
    - pip install -r requirements.txt
    - pip install pytest pycodestyle

script:
    - pycodestyle . --max-line-length=120
    - pytest
